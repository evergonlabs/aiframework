#!/usr/bin/env bash
set -euo pipefail
echo "Pre-commit: checking..."
if command -v cargo &>/dev/null && [ -d rust ]; then
  cd rust && cargo check --quiet 2>/dev/null && echo "Rust check passed." || echo "Warning: cargo check failed"
fi
