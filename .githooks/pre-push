#!/usr/bin/env bash
set -euo pipefail
echo "Pre-push: running tests..."
if command -v cargo &>/dev/null && [ -d rust ]; then
  cd rust && cargo test --quiet 2>/dev/null && echo "All tests passed." || { echo "Tests failed!"; exit 1; }
fi
